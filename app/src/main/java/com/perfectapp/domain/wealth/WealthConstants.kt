package com.perfectapp.domain.wealth

/** Base currency all net worth figures are converted into. A future Settings phase
 *  can make this user-configurable and persist it via DataStore. */
const val BASE_CURRENCY = "USD"
